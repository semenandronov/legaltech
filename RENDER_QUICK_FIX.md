# 🚨 БЫСТРОЕ ИСПРАВЛЕНИЕ ДЛЯ RENDER

## Проблема
Render пытается собрать проект как Node.js, но нужен Python.

## Решение (через Render Dashboard)

### 1. Откройте ваш сервис в Render Dashboard

### 2. Перейдите в Settings

### 3. Измените эти настройки:

**Runtime**: 
- Сейчас: Node.js
- Должно быть: **Python 3** ← ВЫБЕРИТЕ ИЗ СПИСКА!

**Root Directory**:
- Сейчас: (пусто или `/`)
- Должно быть: **backend**

**Build Command**:
- Сейчас: `yarn install; yarn build`
- Должно быть: **`pip install -r requirements.txt`**

**Start Command**:
- Сейчас: `yarn start` или что-то подобное
- Должно быть: **`uvicorn app.main:app --host 0.0.0.0 --port $PORT`**

### 4. Сохраните изменения

### 5. Нажмите "Manual Deploy" → "Deploy latest commit"

### 6. Проверьте логи

Должны увидеть:
```
Installing dependencies...
pip install -r requirements.txt
...
Starting uvicorn...
```

Если видите `yarn install` → настройки не применились, проверьте еще раз.

## Альтернатива: Создать новый сервис

Если не получается изменить существующий:

1. **Удалите** старый сервис
2. **Создайте новый** Web Service
3. При создании **сразу выберите Python 3** в Runtime
4. Укажите Root Directory: `backend`

