# Откуда SDK берет folder_id, если он не указан явно?

## Проблема

В логах появляется папка `b1gi8radi6m4pfdqvq56`, которая нигде не указана в коде, хотя в `YANDEX_FOLDER_ID` указана папка `b1g4samml2s1n1509ptp`.

## Возможные источники folder_id

### 1. API ключ может содержать информацию о папке по умолчанию

**Гипотеза:** Yandex Cloud API ключи могут быть привязаны к определенной папке, и SDK автоматически извлекает эту информацию.

**Проверка:**
- Откройте Yandex Cloud Console → IAM → Service Accounts
- Найдите сервисный аккаунт "layer"
- Проверьте, к какой папке привязан API ключ
- Возможно, API ключ был создан в папке `b1gi8radi6m4pfdqvq56`

### 2. Переменные окружения, которые мы не видим

**Проверьте в Render:**
- Может быть несколько переменных с похожими именами
- Проверьте все переменные, содержащие `FOLDER` или `YANDEX`

### 3. SDK может использовать folder_id из контекста API ключа

**Гипотеза:** SDK может автоматически определять `folder_id` из самого API ключа через вызов к Yandex Cloud API.

**Как проверить:**
- SDK может делать запрос к `/iam/v1/tokens:validate` или подобному endpoint
- И оттуда получать информацию о папке, к которой привязан ключ

### 4. Кэш или конфигурация SDK

**Малозначительно:** SDK не должен кэшировать folder_id, но стоит проверить.

## Решение: Явное указание folder_id

✅ **Текущий код уже явно указывает folder_id:**

```python
self.sdk = YCloudML(folder_id=self.folder_id, auth=auth)
```

Но SDK может игнорировать этот параметр, если:
- API ключ привязан к другой папке
- SDK делает дополнительный запрос для валидации и получает другую папку

## Что нужно проверить

### 1. Проверьте, к какой папке привязан API ключ

1. Откройте: https://console.cloud.yandex.ru/iam/service-accounts
2. Найдите сервисный аккаунт "layer" (ID: `aje4d8448d6mf9lbcukf`)
3. Перейдите на вкладку "API ключи"
4. Найдите API ключ с ID `ajehpdvj72jg5qr8q1ie`
5. Проверьте, есть ли информация о папке по умолчанию

### 2. Проверьте все переменные окружения в Render

В Render Dashboard → Environment проверьте:
- `YANDEX_FOLDER_ID` - должно быть `b1g4samml2s1n1509ptp`
- Нет ли других переменных типа `FOLDER_ID`, `YANDEX_FOLDER`, и т.д.

### 3. Проверьте логи при старте приложения

После деплоя в логах должно быть:
```
✅ Yandex Cloud ML SDK initialized for Vector Store with folder_id: b1g4samml2s1n1509ptp
📋 Using folder_id from environment: b1g4samml2s1n1509ptp
```

Если в логах другое значение - значит переменная окружения установлена неправильно.

### 4. Проверьте, не создан ли API ключ в другой папке

Возможно, API ключ был создан в папке `b1gi8radi6m4pfdqvq56`, и SDK использует эту информацию.

**Решение:** Создайте новый API ключ в нужной папке `b1g4samml2s1n1509ptp`.

## Альтернативное решение: Проверка folder_id в SDK

Если SDK все равно использует другую папку, можно попробовать:

1. Явно передавать `folder_id` во все вызовы SDK
2. Проверять ответы от SDK на предмет использования правильной папки
3. Использовать IAM токен вместо API ключа (но это менее удобно)

## Временное решение

Пока не выяснено, откуда берется `b1gi8radi6m4pfdqvq56`:

1. **Убедитесь, что `YANDEX_FOLDER_ID` в Render = `b1g4samml2s1n1509ptp`**
2. **Проверьте логи при старте** - какой folder_id выводится
3. **Попробуйте создать новый API ключ** в нужной папке

Если проблема сохранится после всех проверок, возможно, SDK действительно игнорирует явно указанный `folder_id` и использует папку из API ключа.

