# 🚨 КРИТИЧЕСКОЕ ИСПРАВЛЕНИЕ: Проблема с RSC 404

## Проблема:
Next.js пытается загрузить страницы как RSC (`?_rsc=1qjny`), но получает 404. Это означает, что **страницы не собрались правильно на Render**.

## Решение:

### 1. Проверьте логи сборки на Render

**ОБЯЗАТЕЛЬНО** проверьте логи сборки (Build Logs) на Render:

1. Откройте ваш сервис на Render Dashboard
2. Перейдите в **"Logs"**
3. Найдите раздел **"Build Logs"** (логи сборки)
4. Ищите строки:
   ```
   Route (app)                              Size     First Load JS
   ┌ ○ /                                    ... kB   ... kB
   ├ ○ /login                               ... kB   ... kB
   ├ ○ /register                            ... kB   ... kB
   ├ ○ /dashboard                           ... kB   ... kB
   ├ ○ /dashboard/summarize                 ... kB   ... kB
   ├ ○ /dashboard/ediscovery                ... kB   ... kB
   ├ ○ /dashboard/timeline                  ... kB   ... kB
   ├ ○ /dashboard/chat                      ... kB   ... kB
   └ ○ /dashboard/tabular                   ... kB   ... kB
   ```

**Если этих строк НЕТ** - страницы не собрались! Нужно исправить ошибки сборки.

### 2. Если есть ошибки сборки:

Исправьте их. Частые проблемы:
- Отсутствующие зависимости
- Ошибки TypeScript
- Проблемы с импортами

### 3. Очистите кэш и пересоберите:

1. На Render: **Settings** → **Clear build cache**
2. **Manual Deploy** → **Deploy latest commit**

### 4. Проверьте переменные окружения:

Убедитесь, что установлены:
- `DATABASE_URL`
- `NEXTAUTH_URL` = `https://legaltech-ynit.onrender.com`
- `NEXTAUTH_SECRET`

**Без этих переменных сборка может пройти, но приложение не будет работать правильно.**

## Важно:

**Проблема НЕ в коде** - все страницы существуют и правильно структурированы. Проблема в **сборке на Render**.

**Пришлите логи сборки**, если проблема не решается - я помогу найти конкретную ошибку.

