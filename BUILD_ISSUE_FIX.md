# 🔧 Исправление проблемы со сборкой (404 на всех страницах)

## Проблема:
Ошибки `?_rsc=1qjny 404` означают, что **страницы не собрались правильно** на Render. Это НЕ связано с DATABASE_URL.

## Решение:

### 1. Проверьте логи сборки на Render

**КРИТИЧЕСКИ ВАЖНО** - проверьте Build Logs:

1. Откройте Render Dashboard → ваш сервис
2. Перейдите в **"Logs"**
3. Найдите раздел **"Build Logs"** (логи сборки, не runtime logs!)
4. Прокрутите до конца логов сборки
5. Ищите строки:
   ```
   Route (app)                              Size     First Load JS
   ┌ ○ /                                    ... kB   ... kB
   ├ ○ /login                               ... kB   ... kB
   ├ ○ /register                            ... kB   ... kB
   ├ ○ /dashboard                           ... kB   ... kB
   ├ ○ /dashboard/summarize                 ... kB   ... kB
   ├ ○ /dashboard/ediscovery                ... kB   ... kB
   ├ ○ /dashboard/timeline                  ... kB   ... kB
   ├ ○ /dashboard/chat                      ... kB   ... kB
   └ ○ /dashboard/tabular                   ... kB   ... kB
   ```

**Если этих строк НЕТ** - страницы не собрались!

### 2. Если страниц нет в списке:

Ищите ошибки в логах сборки:
- `Error:` или `Failed to compile`
- `Cannot find module`
- `Type error`
- Любые другие ошибки

### 3. Очистите кэш и пересоберите:

1. **Settings** → **Clear build cache**
2. **Manual Deploy** → **Deploy latest commit**

### 4. Проверьте build command на Render:

Должно быть:
```
yarn install && yarn build
```

Или:
```
npm install && npm run build
```

### 5. Если ничего не помогает:

**Пришлите полные логи сборки** (Build Logs) - я помогу найти конкретную ошибку.

## Важно:

Проблема **НЕ в коде** - все страницы существуют и правильно структурированы. Проблема в **сборке на Render**.

