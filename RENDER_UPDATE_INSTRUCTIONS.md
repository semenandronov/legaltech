# 🔧 Инструкция по обновлению сервиса на Render

## Проблема

Сейчас на Render развернут старый сервис, который не раздает frontend. Нужно обновить настройки.

## Решение: Обновить существующий сервис

### Шаг 1: Откройте ваш сервис в Render Dashboard

Зайдите на https://dashboard.render.com и найдите сервис `legaltech-ynit` (или как он называется)

### Шаг 2: Перейдите в Settings

### Шаг 3: Измените настройки

**Root Directory**:
- Сейчас: `backend` или пусто
- Должно быть: `.` (точка - корень проекта)

**Build Command**:
- Сейчас: `pip install -r requirements.txt` или что-то подобное
- Должно быть: 
```bash
pip install -r backend/requirements.txt && cd frontend && npm install && npm run build
```

**Start Command**:
- Сейчас: `uvicorn app.main:app --host 0.0.0.0 --port $PORT`
- Должно быть:
```bash
cd backend && uvicorn app.main:app --host 0.0.0.0 --port $PORT
```

### Шаг 4: Сохраните изменения

### Шаг 5: Manual Deploy

Нажмите "Manual Deploy" → "Deploy latest commit"

### Шаг 6: Проверьте логи

В логах должны увидеть:
1. Установку Python зависимостей
2. Установку npm пакетов
3. Сборку frontend (`npm run build`)
4. Запуск uvicorn

## Альтернатива: Создать новый сервис

Если не получается обновить существующий:

1. **Удалите** старый сервис
2. **Создайте новый** Web Service
3. При создании используйте настройки из `render.yaml`:
   - Runtime: Python 3
   - Root Directory: `.`
   - Build Command: `pip install -r backend/requirements.txt && cd frontend && npm install && npm run build`
   - Start Command: `cd backend && uvicorn app.main:app --host 0.0.0.0 --port $PORT`

## Проверка после деплоя

После успешного деплоя:
- `https://your-app.onrender.com/` - должна открыться главная страница (frontend)
- `https://your-app.onrender.com/api/health` - должен вернуть `{"status": "ok"}`

**Важно**: `/dashboard` больше не существует! Главная страница - это `/` (корень).

