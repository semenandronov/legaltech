# Инструкция по запуску в Replit

## Быстрый старт

1. **Импортируйте репозиторий в Replit**
   - Перейдите на https://replit.com
   - Нажмите "Import from GitHub"
   - Вставьте URL: `https://github.com/semenandronov/legaltech.git`

2. **Установите зависимости**
   ```bash
   npm install
   ```

3. **Настройте переменные окружения**
   - Перейдите в раздел "Secrets" (🔒) в боковой панели Replit
   - Добавьте следующие переменные:
     - `DATABASE_URL` - URL вашей PostgreSQL базы данных
     - `NEXTAUTH_URL` - URL вашего Replit проекта (например: `https://your-project.repl.co`)
     - `NEXTAUTH_SECRET` - случайная строка (можно сгенерировать: `openssl rand -base64 32`)
     - `OPENAI_API_KEY` - (опционально) ключ OpenAI API
     - `YANDEX_API_KEY` - (опционально) ключ Yandex Cloud API
     - `YANDEX_FOLDER_ID` - (опционально) Folder ID из Yandex Cloud

4. **Настройте базу данных**
   
   **Вариант 1: Внешняя БД (рекомендуется)**
   - Используйте бесплатные сервисы:
     - [Supabase](https://supabase.com) - бесплатный PostgreSQL
     - [Neon](https://neon.tech) - бесплатный PostgreSQL
     - [Railway](https://railway.app) - бесплатный tier
   - Скопируйте connection string в `DATABASE_URL`

   **Вариант 2: SQLite для разработки**
   - Измените `provider` в `prisma/schema.prisma` на `"sqlite"`
   - Удалите поле `embedding` из модели `Document` (не поддерживается в SQLite)
   - Измените `DATABASE_URL` на `"file:./dev.db"`

5. **Выполните миграции**
   ```bash
   npx prisma migrate dev --name init
   npx prisma generate
   ```

6. **Запустите проект**
   ```bash
   npm run dev
   ```

   Или просто нажмите кнопку "Run" в Replit

## Важные замечания

### База данных
- Replit не предоставляет PostgreSQL по умолчанию
- Используйте внешний сервис или SQLite для разработки
- Для продакшна рекомендуется использовать внешнюю БД

### Переменные окружения
- Все переменные должны быть добавлены в Secrets
- `NEXTAUTH_URL` должен быть полным URL вашего Replit проекта
- `NEXTAUTH_SECRET` должен быть случайной строкой минимум 32 символа

### Порты
- Replit автоматически назначает порт
- Используйте переменную `PORT` из окружения (если доступна)
- Next.js автоматически использует порт из `PORT` или 3000 по умолчанию

### Ограничения Replit
- Максимальный размер файла: 10MB
- Таймауты для длительных операций могут быть ограничены
- Для больших документов используйте внешние сервисы

## Troubleshooting

### Ошибка при установке зависимостей
```bash
rm -rf node_modules package-lock.json
npm install
```

### Ошибка Prisma
```bash
npx prisma generate
npx prisma migrate dev
```

### Ошибка сборки Next.js
```bash
npm run lint
npm run build
```

### Проблемы с портом
- Replit автоматически назначает порт
- Проверьте переменную `PORT` в Secrets
- Убедитесь, что `NEXTAUTH_URL` соответствует вашему URL

## Проверка готовности

Запустите проверку:
```bash
npm run check
```

Это проверит все необходимые файлы и конфигурацию.

## Деплой

Для деплоя в Replit:
1. Убедитесь, что все переменные окружения настроены
2. Нажмите кнопку "Deploy" в Replit
3. Или используйте команду: `npm run build && npm start`

## Поддержка

Если возникли проблемы:
1. Проверьте логи в консоли Replit
2. Убедитесь, что все переменные окружения настроены
3. Проверьте, что база данных доступна
4. Запустите `npm run check` для диагностики

