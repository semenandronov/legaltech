# Анализ текущего использования Yandex в проекте vs Рекомендуемый функционал

## 📊 Сравнительная таблица

| Компонент | Текущее состояние | Рекомендуемое | Статус |
|-----------|-------------------|---------------|--------|
| **1. Модели генерации (YandexGPT)** | ✅ Через ML SDK | ✅ Через ML SDK/API | ✅ **Работает** |
| **2. Vector Store / Search Index** | ❌ Chroma (локально) | ✅ Yandex AI Studio Index | ⚠️ **Требуется миграция** |
| **3. AI Assistant / Agent** | ❌ Самописный RAGService | ✅ Yandex AI Studio Assistant | ⚠️ **Требуется миграция** |
| **4. MCP / Внешние источники** | ❌ Нет | ✅ MCP серверы для ФНС, Росфинмониторинг | ❌ **Требуется добавить** |
| **5. ML SDK** | ✅ Используется | ✅ Используется | ✅ **Работает** |

---

## 🔍 Детальный анализ

### ✅ 1. Модели генерации (YandexGPT / Alice AI LLM)

**Текущая реализация:**

```python
# backend/app/services/yandex_llm.py
class ChatYandexGPT(BaseChatModel):
    def __init__(self):
        self.sdk = YCloudML(folder_id=self.folder_id, auth=auth)
        # Использует: self.sdk.models.completions(self.model_name)
```

**Где используется:**
- ✅ `RAGService` - генерация ответов в чате
- ✅ `PlanningAgent` - планирование задач
- ✅ Все LangGraph агенты (9 файлов):
  - `privilege_check_node.py`
  - `entity_extraction_node.py`
  - `summary_node.py`
  - `discrepancy_node.py`
  - `risk_node.py`
  - `key_facts_node.py`
  - `supervisor.py`
  - `timeline_node.py`
  - `document_classifier_node.py` (fallback)

**Статус:** ✅ **Полностью соответствует требованиям**

---

### ❌ 2. Vector Store / Search Index для RAG

**Текущая реализация:**

```python
# backend/app/services/document_processor.py (строки 145-154)
vector_store = Chroma.from_documents(
    documents=documents,
    embedding=self.embeddings,  # YandexEmbeddings используется
    persist_directory=persist_directory  # Локальное хранилище
)
```

**Проблемы:**
- ❌ Используется локальная БД Chroma (файлы на диске)
- ❌ Хранится в `vector_db/chroma_{case_id}/`
- ❌ Нет централизованного управления
- ❌ Нет масштабируемости между серверами
- ❌ Сложно делать backup и восстановление

**Рекомендуемое:**

```python
# Должно быть через Yandex AI Studio Index API
index = yandex_sdk.indexes.create(
    name=f"case_{case_id}",
    config={...}
)
index.add_documents(documents)
results = index.search(query, k=5)
```

**Что нужно изменить:**
1. Изучить Yandex AI Studio Index API
2. Создать `YandexIndexService`
3. Заменить Chroma на Yandex Index в `document_processor.py`
4. Мигрировать существующие индексы

**Статус:** ⚠️ **Требуется миграция на Yandex AI Studio Index**

---

### ❌ 3. AI Assistant / Agent (RAG-ассистент)

**Текущая реализация:**

```python
# backend/app/services/rag_service.py (упрощенная версия)
class RAGService:
    def generate_answer(self, case_id, query):
        # 1. Ручной поиск документов
        docs = self.retrieve_context(case_id, query)
        
        # 2. Ручное формирование промпта
        prompt = self._format_prompt(query, docs)
        
        # 3. Вызов LLM
        response = self.llm.invoke(prompt)
        
        # 4. Ручное форматирование источников
        sources = self.format_sources(docs)
        
        return response, sources
```

**Проблемы:**
- ❌ Вся логика написана вручную
- ❌ Много кода для поддержки (200+ строк)
- ❌ Нет использования Assistant API
- ❌ Сложно добавлять новые функции (tools, memory и т.д.)

**Рекомендуемое:**

```python
# Должно быть через Yandex AI Studio Assistant API
assistant = yandex_sdk.assistants.create(
    name=f"case_{case_id}",
    model="yandexgpt-pro",
    tools=[
        {"type": "vector_store", "index_id": index_id}
    ]
)

# Просто отправляем сообщение - ассистент сам:
# - делает поиск по индексу
# - формирует промпт
# - генерирует ответ
# - возвращает источники
response = assistant.chat(message, history)
```

**Что нужно изменить:**
1. Изучить Yandex AI Studio Assistant API
2. Создать `YandexAssistantService`
3. Переписать `RAGService` на использование Assistant API
4. Упростить `routes/chat.py`

**Статус:** ⚠️ **Требуется миграция на Yandex AI Studio Assistant API**

---

### ❌ 4. MCP / Интеграция с внешними юридическими источниками

**Текущая реализация:**

```python
# НЕТ РЕАЛИЗАЦИИ
# Нет интеграций с:
# - ФНС (проверка компаний)
# - Росфинмониторинг (санкции)
# - Судебные базы (решения судов)
```

**Рекомендуемое:**

```python
# Должно быть через MCP Tools в Assistant
assistant = yandex_sdk.assistants.create(
    tools=[
        {"type": "vector_store", "index_id": index_id},
        {"type": "mcp", "server": "fns", "functions": ["get_company_info"]},
        {"type": "mcp", "server": "rospil", "functions": ["check_sanctions"]},
        {"type": "mcp", "server": "court_db", "functions": ["search_decisions"]}
    ]
)

# Ассистент сам вызывает tools когда нужно:
# User: "Проверь контрагента ООО Ромашка по ИНН 1234567890"
# Assistant: [вызывает fns.get_company_info(inn="1234567890")]
#            [возвращает: "ООО Ромашка зарегистрировано 01.01.2020, ОКВЭД ..."]
```

**Что нужно добавить:**
1. Настроить MCP серверы для юридических источников
2. Создать клиенты для интеграций:
   - `FNSClient` - проверка компаний по ИНН
   - `RosfinmonitoringClient` - проверка санкций
   - `CourtDatabaseClient` - поиск судебных решений
3. Добавить MCP tools в конфигурацию ассистента
4. Обновить промпты ассистента

**Статус:** ❌ **Требуется полная реализация**

---

### ✅ 5. ML SDK (Yandex Cloud ML SDK)

**Текущая реализация:**

```python
# backend/app/services/yandex_llm.py
from yandex_cloud_ml_sdk import YCloudML
from yandex_cloud_ml_sdk.auth import APIKeyAuth

sdk = YCloudML(folder_id=folder_id, auth=auth)
model = sdk.models.completions(model_name)
result = model.run(messages)
```

**Где используется:**
- ✅ `yandex_llm.py` - для YandexGPT
- ✅ `yandex_embeddings.py` - для embeddings

**Статус:** ✅ **Полностью соответствует требованиям**

---

## 📈 Метрики текущей реализации

### Код, который нужно изменить/заменить:

1. **Vector Store:**
   - `document_processor.py` - ~150 строк (Chroma → Yandex Index)
   - `langchain_retrievers.py` - ~50 строк (адаптация)
   - **Всего:** ~200 строк кода

2. **AI Assistant:**
   - `rag_service.py` - ~300 строк (полная переработка)
   - `routes/chat.py` - ~100 строк (упрощение)
   - **Всего:** ~400 строк кода

3. **MCP интеграции:**
   - Новые файлы: ~500 строк кода
   - Интеграция в ассистента: ~100 строк
   - **Всего:** ~600 строк кода

**Итого изменений:** ~1200 строк кода

---

## 🎯 Приоритеты миграции

### Высокий приоритет (критично для e-discovery):

1. **Vector Store → Yandex AI Studio Index**
   - Основа RAG системы
   - Без этого невозможно масштабирование
   - **Время:** 2-3 дня

2. **RAGService → Yandex AI Studio Assistant**
   - Упрощение и надежность
   - Лучшая интеграция с индексом
   - **Время:** 3-4 дня

### Средний приоритет (важно для полного функционала):

3. **MCP интеграции**
   - Уникальная фича для legaltech
   - Автоматическая проверка контрагентов
   - **Время:** 5-7 дней

---

## 💡 Рекомендации

### Немедленные действия:

1. **Изучить документацию Yandex AI Studio:**
   - Index API: https://yandex.cloud/en/docs/ai-studio/operations/index/
   - Assistant API: https://yandex.cloud/en/docs/ai-studio/operations/assistant/

2. **Создать тестовый проект:**
   - Попробовать создать индекс вручную через консоль
   - Попробовать создать ассистента с индексом
   - Проверить работу через API

3. **Начать миграцию Vector Store:**
   - Самая критичная часть
   - Остальной функционал зависит от этого

### Долгосрочные улучшения:

1. **Мониторинг и аналитика:**
   - Интегрировать мониторинг использования Yandex AI Studio
   - Отслеживать costs
   - Оптимизировать запросы

2. **Кеширование:**
   - Кешировать частые запросы
   - Уменьшить costs

3. **Тестирование:**
   - Unit тесты для новых сервисов
   - Integration тесты с Yandex AI Studio
   - Performance тесты

---

## 🔗 Связанные документы

- [План миграции](./YANDEX_AI_STUDIO_MIGRATION_PLAN.md) - детальный план реализации
- [Как используется Yandex](./HOW_YANDEX_IS_USED.md) - текущая архитектура
- [Интеграция Yandex](./YANDEX_INTEGRATION.md) - общая информация

