# ⚠️ ВАЖНО: YANDEX_FOLDER_ID обязателен!

## 🚨 Проблема

Официальный Yandex Cloud ML SDK **требует** `folder_id` для работы. Он не может работать без него, даже если есть API ключ.

## ✅ Решение

**Обязательно добавьте `YANDEX_FOLDER_ID` в Render!**

### Что добавить в Render:

```
YANDEX_API_KEY=AQVNxxxxxxxxxxxxx
YANDEX_FOLDER_ID=b1g4samml2s1n1509ptp  # ⚠️ ОБЯЗАТЕЛЬНО!
```

### Где взять Folder ID:

1. Откройте Yandex Cloud Console: https://console.cloud.yandex.ru
2. Выберите папку (например, "legaltech" или "default")
3. В URL или в настройках папки найдите ID
   - Формат: `b1g4samml2s1n1509ptp`
   - Или в терминале: `yc config get folder-id`

## 📝 Полный список переменных для Render

### Обязательные для Яндекс:
```
YANDEX_API_KEY=AQVNxxxxxxxxxxxxx
YANDEX_FOLDER_ID=b1g4samml2s1n1509ptp  # ⚠️ ОБЯЗАТЕЛЬНО!
```

### Опциональные:
```
YANDEX_AI_STUDIO_CLASSIFIER_ID=classifier-xxxxx
YANDEX_GPT_MODEL=yandexgpt-pro/latest
```

## 🔄 Что происходит без folder_id

Если `YANDEX_FOLDER_ID` не указан:
- ❌ SDK не инициализируется
- ✅ Система автоматически использует OpenRouter (fallback)
- ✅ Всё работает, но дороже

## ✅ После добавления folder_id

После добавления `YANDEX_FOLDER_ID` в Render:
- ✅ SDK инициализируется успешно
- ✅ Используется YandexGPT (экономия 90%)
- ✅ Используются Yandex Embeddings (лучше для русского)

## 🎯 Итог

**Folder ID обязателен!** Без него Яндекс не будет работать, но система продолжит работать через OpenRouter.

**Добавь в Render:**
```
YANDEX_API_KEY=AQVNxxxxxxxxxxxxx
YANDEX_FOLDER_ID=b1g4samml2s1n1509ptp  # Из твоего скриншота!
```

После этого всё заработает! 🎉
