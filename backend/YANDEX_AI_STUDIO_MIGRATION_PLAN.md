# План миграции на полный функционал Yandex AI Studio

## 📊 Текущее состояние vs Рекомендуемый функционал

### ✅ Что уже реализовано:

1. **Модели генерации (YandexGPT)**
   - ✅ Используется через ML SDK (`yandex-cloud-ml-sdk`)
   - ✅ Интегрировано в RAGService, PlanningAgent, все LangGraph агенты
   - ✅ Поддержка API ключа и IAM токена
   - **Статус:** Полностью работает

2. **ML SDK**
   - ✅ `yandex-cloud-ml-sdk` установлен и используется
   - ✅ Интеграция с LangChain через кастомные классы
   - **Статус:** Полностью работает

---

### ⚠️ Что нужно мигрировать/добавить:

#### 1. Vector Store / Search Index ❌ → ✅

**Текущее состояние:**
- Используется локальная БД **Chroma** (файловая система)
- Хранится в `vector_db/chroma_{case_id}/`
- Нет централизованного управления индексами
- Нет масштабируемости между серверами

**Рекомендуемое:**
- Использовать **Yandex AI Studio Vector Store / Search Index**
- Централизованное хранилище в облаке
- Автоматическое масштабирование
- Интеграция с Assistant API

**Файлы для изменения:**
- `backend/app/services/document_processor.py` (строки 145-154, 217-243)
- `backend/app/services/langchain_retrievers.py`
- Заменить Chroma на Yandex AI Studio Index API

---

#### 2. AI Assistant / Agent ❌ → ✅

**Текущее состояние:**
- Самописный `RAGService` с ручной логикой:
  - Поиск документов → Формирование промпта → Вызов LLM
  - Нет использования Assistant API

**Рекомендуемое:**
- Использовать **Yandex AI Studio Assistant API**
- Создать ассистента с Vector Store tool (автоматический поиск по индексу)
- Упростить логику: просто отправка сообщений ассистенту

**Файлы для изменения:**
- `backend/app/services/rag_service.py` (полная переработка)
- Создать новый `backend/app/services/yandex_assistant.py`

---

#### 3. MCP / Интеграция с внешними источниками ❌ → ✅

**Текущее состояние:**
- Нет интеграций с внешними источниками
- Нет проверок ФНС, Росфинмониторинг, судебные базы

**Рекомендуемое:**
- Настроить **MCP серверы** для юридических источников
- Интегрировать через Assistant Tools
- Примеры источников:
  - ФНС (по ИНН/OGRN)
  - Росфинмониторинг (санкции)
  - Судебные базы (решения судов)

**Новые файлы:**
- `backend/app/services/mcp_integrations/` (новая папка)
- `backend/app/services/mcp_integrations/fns_client.py`
- `backend/app/services/mcp_integrations/rospil_client.py`
- `backend/app/services/mcp_integrations/court_db_client.py`

---

## 🔄 Детальный план миграции

### Этап 1: Миграция Vector Store на Yandex AI Studio Index

**Приоритет:** Высокий  
**Сложность:** Средняя  
**Время:** 2-3 дня

#### Шаги:

1. **Изучить Yandex AI Studio Index API**
   - Документация: https://yandex.cloud/en/docs/ai-studio/operations/index/
   - Методы: создание, загрузка документов, поиск

2. **Создать новый сервис `yandex_index.py`**
   ```python
   # backend/app/services/yandex_index.py
   class YandexIndexService:
       - create_index(case_id, config)
       - add_documents(index_id, documents)
       - search(index_id, query, k)
       - delete_index(index_id)
   ```

3. **Обновить `document_processor.py`**
   - Заменить Chroma на YandexIndexService
   - Сохранить интерфейс для обратной совместимости
   - Мигрировать существующие индексы (опционально)

4. **Тестирование**
   - Проверить создание индексов
   - Проверить поиск
   - Сравнить качество с Chroma

---

### Этап 2: Интеграция AI Assistant API

**Приоритет:** Высокий  
**Сложность:** Средняя  
**Время:** 3-4 дня

#### Шаги:

1. **Изучить Yandex AI Studio Assistant API**
   - Документация: https://yandex.cloud/en/docs/ai-studio/operations/assistant/
   - Создание ассистента с Vector Store tool
   - Chat API для диалога

2. **Создать сервис `yandex_assistant.py`**
   ```python
   # backend/app/services/yandex_assistant.py
   class YandexAssistantService:
       - create_assistant(case_id, index_id, config)
       - send_message(assistant_id, message, history)
       - get_assistant(assistant_id)
       - delete_assistant(assistant_id)
   ```

3. **Обновить `rag_service.py`**
   - Заменить ручную логику на вызовы Assistant API
   - Упростить код (ассистент сам делает поиск и генерацию)
   - Сохранить форматирование источников

4. **Обновить `routes/chat.py`**
   - Использовать новый YandexAssistantService
   - Упростить обработку ответов

5. **Тестирование**
   - Проверить создание ассистентов
   - Проверить диалог
   - Проверить качество ответов и источников

---

### Этап 3: Добавление MCP интеграций

**Приоритет:** Средний  
**Сложность:** Высокая  
**Время:** 5-7 дней

#### Шаги:

1. **Изучить MCP протокол и доступные серверы**
   - Документация: https://mcpservers.org/
   - Найти готовые MCP серверы для юридических источников
   - Или создать свои

2. **Настроить MCP серверы**
   - ФНС API (проверка компаний по ИНН)
   - Росфинмониторинг (проверка санкций)
   - Судебные базы (поиск решений)

3. **Интегрировать через Assistant Tools**
   - Добавить MCP tools в конфигурацию ассистента
   - Обучить ассистента использовать tools

4. **Создать сервисы интеграций**
   ```python
   # backend/app/services/mcp_integrations/fns_client.py
   class FNSClient:
       - get_company_info(inn)
       - check_company_status(inn)
   
   # backend/app/services/mcp_integrations/rospil_client.py
   class RosfinmonitoringClient:
       - check_sanctions(company_name, inn)
   
   # backend/app/services/mcp_integrations/court_db_client.py
   class CourtDatabaseClient:
       - search_decisions(company_name, period)
   ```

5. **Обновить промпты ассистента**
   - Добавить инструкции по использованию tools
   - Примеры использования

6. **Тестирование**
   - Проверить вызовы tools
   - Проверить интеграцию с ассистентом
   - Проверить качество ответов с внешними данными

---

## 📝 Детали реализации

### 1. Yandex AI Studio Index Service

```python
# backend/app/services/yandex_index.py
from yandex_cloud_ml_sdk import YCloudML
from yandex_cloud_ml_sdk.auth import APIKeyAuth
from app.config import config
import logging

logger = logging.getLogger(__name__)

class YandexIndexService:
    """Service for Yandex AI Studio Vector Store / Search Index"""
    
    def __init__(self):
        auth = APIKeyAuth(config.YANDEX_API_KEY) if config.YANDEX_API_KEY else config.YANDEX_IAM_TOKEN
        self.sdk = YCloudML(folder_id=config.YANDEX_FOLDER_ID, auth=auth)
    
    def create_index(self, case_id: str, name: str = None):
        """Create new search index for case"""
        # TODO: Использовать SDK для создания индекса
        # index = self.sdk.indexes.create(...)
        pass
    
    def add_documents(self, index_id: str, documents: List[Document]):
        """Add documents to index"""
        # TODO: Загрузить документы в индекс
        pass
    
    def search(self, index_id: str, query: str, k: int = 5):
        """Search documents in index"""
        # TODO: Выполнить поиск
        pass
```

### 2. Yandex AI Studio Assistant Service

```python
# backend/app/services/yandex_assistant.py
from yandex_cloud_ml_sdk import YCloudML
from app.config import config
import logging

logger = logging.getLogger(__name__)

class YandexAssistantService:
    """Service for Yandex AI Studio Assistant API"""
    
    def __init__(self):
        auth = APIKeyAuth(config.YANDEX_API_KEY) if config.YANDEX_API_KEY else config.YANDEX_IAM_TOKEN
        self.sdk = YCloudML(folder_id=config.YANDEX_FOLDER_ID, auth=auth)
    
    def create_assistant(self, case_id: str, index_id: str, config: dict = None):
        """Create AI assistant with vector store tool"""
        # TODO: Создать ассистента с Vector Store tool
        # assistant = self.sdk.assistants.create(...)
        pass
    
    def send_message(self, assistant_id: str, message: str, history: List = None):
        """Send message to assistant"""
        # TODO: Отправить сообщение через Chat API
        # response = self.sdk.assistants.chat(assistant_id, message, history)
        pass
```

---

## 🎯 Преимущества миграции

### После миграции:

1. **Масштабируемость**
   - Централизованное хранилище вместо локальных файлов
   - Можно масштабировать на несколько серверов

2. **Упрощение кода**
   - Меньше кода в RAGService (ассистент сам делает поиск)
   - Меньше багов и edge cases

3. **Лучшее качество**
   - Оптимизированный поиск от Yandex
   - Автоматическая релевантность
   - Поддержка метаданных

4. **Интеграция с внешними источниками**
   - Автоматическая проверка компаний
   - Проверка санкций
   - Поиск судебных решений

5. **Мониторинг и аналитика**
   - Встроенная аналитика Yandex AI Studio
   - Логирование использования
   - Оптимизация costs

---

## ⚠️ Риски и митигация

### Риски:

1. **Изменение API Yandex AI Studio**
   - Митигация: Версионирование API, fallback на старую реализацию

2. **Стоимость использования**
   - Митигация: Мониторинг costs, оптимизация запросов

3. **Производительность**
   - Митигация: Кеширование, асинхронные запросы

4. **Миграция существующих данных**
   - Митигация: Постепенная миграция, поддержка обеих реализаций временно

---

## 📅 Примерный timeline

- **Неделя 1:** Этап 1 (Vector Store миграция)
- **Неделя 2:** Этап 2 (Assistant API интеграция)
- **Неделя 3-4:** Этап 3 (MCP интеграции)
- **Неделя 5:** Тестирование, оптимизация, документация

**Общее время:** 4-5 недель

---

## 🚀 Quick Start (для начала работы)

1. **Изучить документацию:**
   - https://yandex.cloud/en/docs/ai-studio/
   - https://yandex.cloud/en/docs/ai-studio/sdk/

2. **Создать тестовый индекс в консоли Yandex AI Studio**

3. **Написать простой тест интеграции**

4. **Постепенно мигрировать функционал**

---

## 📚 Полезные ссылки

- [Yandex AI Studio Operations](https://yandex.cloud/en/docs/ai-studio/operations/)
- [Yandex AI Studio SDK](https://yandex.cloud/en/docs/ai-studio/sdk/)
- [ML SDK PyPI](https://pypi.org/project/yandex-cloud-ml-sdk/)
- [MCP Servers](https://mcpservers.org/)
- [YandexGPT Concepts](https://yandex.cloud/en/docs/yandexgpt/concepts/)



