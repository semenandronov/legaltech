fastapi==0.115.0
uvicorn[standard]==0.32.0
python-multipart==0.0.12
sqlalchemy==2.0.36
openai==2.13.0
python-dotenv==1.0.1
pypdf==4.3.1
aiofiles==24.1.0
pydantic==2.9.2
email-validator==2.3.0
psycopg2-binary==2.9.10
openpyxl==3.1.5
python-jose[cryptography]==3.5.0
passlib[bcrypt]==1.7.4
bcrypt==5.0.0
langchain==1.2.0
langchain-openai==1.1.5
langchain-community==0.4.1
langchain-core==1.2.2
langchain-classic==1.0.0
# langchain-postgres temporarily removed due to incompatibility with langchain-core 1.2.2
# Will use direct pgvector approach via SQLAlchemy
# langchain-postgres==0.0.16  # Requires langchain-core<0.3, incompatible with 1.2.2
langgraph==1.0.5
langgraph-checkpoint-postgres==3.0.2
pgvector==0.3.4  # PostgreSQL vector extension Python bindings
unstructured==0.18.21
chromadb==1.3.7
python-docx==1.2.0
reportlab==4.4.6
jsonlines>=4.0.0
# Yandex Cloud services
yandex-cloud-ml-sdk==0.17.1  # Официальный SDK для YandexGPT и Embeddings
yandexcloud>=0.359.0  # Обновлено для совместимости с yandex-cloud-ml-sdk (требует >=0.359.0)
requests>=2.32.5  # Обновлено для совместимости с langchain-community

# GigaChat (Сбер) - используем кастомный wrapper (совместим с langchain-core 1.2.2)
# langchain-gigachat требует langchain-core<0.4, что несовместимо с нашими версиями
gigachat>=0.1.0  # Официальный SDK для GigaChat (используется в нашем кастомном wrapper)
# langchain-gigachat>=0.1.0  # Несовместим с langchain-core==1.2.2, используем кастомный wrapper

# LexNLP - библиотека для извлечения юридически значимых данных
# Требует Python 3.11 или 3.12 (не совместим с 3.13 из-за gensim 4.1.2)
# Код поддерживает работу без LexNLP через regex fallbacks, если библиотека недоступна
lexnlp>=0.3.0

# BM25 - алгоритм для sparse search (hybrid search)
rank-bm25>=0.2.2

