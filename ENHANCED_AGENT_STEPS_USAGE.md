# Руководство по использованию улучшенной визуализации цепочки действий

## Что было улучшено

На основе изучения практик Vercel AI SDK и других современных библиотек для чатов с ИИ, был создан улучшенный компонент `EnhancedAgentStepsView`, который предоставляет:

### ✨ Новые возможности:

1. **Коллапсируемые секции** - можно свернуть/развернуть детали каждого шага
2. **Визуализация tool calls** - отдельные блоки для каждого вызова инструмента
3. **Временные метки** - отображение времени выполнения и длительности
4. **Структурированные данные** - красивое форматирование JSON
5. **Анимации** - плавные переходы при изменении статусов
6. **Иконки** - визуальное различие типов действий (рассуждения, инструменты, результаты)

## Использование

### Базовое использование (уже интегрировано):

```tsx
<EnhancedAgentStepsView 
  steps={agentSteps} 
  isStreaming={isLoading}
/>
```

### С настройками:

```tsx
<EnhancedAgentStepsView 
  steps={agentSteps} 
  isStreaming={isLoading}
  showReasoning={true}      // Показывать рассуждения
  collapsible={true}         // Разрешить сворачивание
/>
```

## Расширенная структура данных

Для полного использования всех возможностей, бэкенд может отправлять расширенную структуру:

```typescript
interface EnhancedAgentStep {
  step_id: string
  agent_name: string
  description: string
  status: 'pending' | 'running' | 'completed' | 'error'
  reasoning?: string          // Рассуждения агента
  result?: string             // Результат выполнения
  error?: string              // Ошибка (если есть)
  timestamp?: string          // Время начала выполнения
  duration?: number           // Длительность в миллисекундах
  tool_calls?: Array<{        // Вызовы инструментов
    name: string
    input?: any
    output?: any
  }>
  input?: any                 // Входные данные (технические)
  output?: any                // Выходные данные (технические)
}
```

## Примеры отображения

### Шаг с рассуждениями:
- Показывается блок с иконкой мозга 🧠
- Рассуждения отображаются в читаемом формате
- Можно свернуть/развернуть

### Шаг с tool calls:
- Каждый вызов инструмента показывается отдельным блоком
- Иконка молнии ⚡ для инструментов
- Входные и выходные данные в раскрывающихся секциях
- JSON форматируется с отступами

### Шаг с результатом:
- Зеленый блок с иконкой документа 📄
- Результат отображается в читаемом формате

### Шаг с ошибкой:
- Красный блок с иконкой предупреждения ⚠️
- Ошибка выделена цветом

### Технические детали:
- Скрыты по умолчанию
- Можно раскрыть для просмотра input/output
- JSON форматируется для удобного чтения

## Интеграция с бэкендом

### Текущая структура (совместима):
Компонент работает с текущей структурой `AgentStep` из `AgentStepsView.tsx`

### Рекомендуемые улучшения бэкенда:

1. **Добавить tool_calls в шаги:**
```python
execution_step = {
    "step_id": step_id,
    "agent_name": agent_name,
    "description": description,
    "status": status,
    "reasoning": reasoning,
    "tool_calls": [
        {
            "name": tool_name,
            "input": tool_input,
            "output": tool_output
        }
    ],
    "duration": duration_ms,
    "timestamp": datetime.now().isoformat()
}
```

2. **Потоковая передача рассуждений:**
```python
# Отправлять части рассуждений по мере генерации
yield f"data: {json.dumps({
    'type': 'reasoning_chunk',
    'step_id': step_id,
    'chunk': reasoning_chunk
})}\n\n"
```

## Следующие шаги

1. ✅ Создан улучшенный компонент
2. ✅ Интегрирован в AssistantUIChat
3. ⏳ Обновить бэкенд для поддержки tool_calls
4. ⏳ Добавить потоковую передачу рассуждений
5. ⏳ Добавить интерактивные функции (остановка, перезапуск)

## Обратная совместимость

Компонент полностью совместим с текущей структурой данных. Все новые поля опциональны и будут использоваться, если доступны.

