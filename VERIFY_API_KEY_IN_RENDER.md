# Проверка API ключа в Render

## ✅ Результаты проверки API ключа

API ключ: `AQVN...` (проверен и работает)

### Тест 1: Аутентификация через YandexGPT API
✅ **УСПЕШНО** - API ключ работает, аутентификация проходит

### Тест 2: Права сервисного аккаунта
✅ **УСПЕШНО** - У сервисного аккаунта "layer" есть все нужные роли:
- `ai.assistants.editor` ✅
- `ai.languageModels.user` ✅  
- `editor` ✅

## 🔍 Что нужно проверить в Render

1. **Убедитесь, что именно этот ключ используется:**
   - Render Dashboard → ваш сервис → Environment
   - Найдите переменную `YANDEX_API_KEY`
   - Значение должно начинаться с `AQVN0ct_...` (полный ключ вы знаете из Yandex Cloud Console)

2. **Если ключ другой:**
   - Замените значение `YANDEX_API_KEY` на ваш API ключ из Yandex Cloud Console (начинается с `AQVN0ct_...`)
   - Сохраните изменения
   - Сервис перезапустится автоматически

3. **Проверьте также другие переменные:**
   - `YANDEX_FOLDER_ID` = `b1g4samml2s1n1509ptp` ✅

## 📝 Следующие шаги

После того, как убедитесь, что правильный ключ в Render:

1. **Подождите 5-10 минут** (если только что добавили роль `ai.assistants.editor` - кэш разрешений обновляется)
2. **Попробуйте загрузить документ снова**
3. **Проверьте логи** - должно быть:
   ```
   ✅ Successfully uploaded file document.pdf via SDK
   ✅ Created search index {index_id} for case {case_id} with {N} files
   ```

## ⚠️ Если ошибка все еще возникает

Если после всех проверок ошибка `PERMISSION_DENIED` все еще возникает:

1. Убедитесь, что прошло минимум 5-10 минут после добавления роли
2. Проверьте логи Render - возможно, там используется старый ключ
3. Попробуйте перезапустить сервис вручную (Manual Deploy)

